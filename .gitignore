# build artifacts
*.o
/cowdiff
/tests/probe

# test scratch -- the test scripts and the fuzz recreate their own fixtures
/.testtmp/

# benchmark scratch -- tests/bench.sh recreates these, and they are large
/.bench/
