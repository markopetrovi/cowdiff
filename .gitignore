# build artifacts
*.o
/cowdiff
/tests/probe

# test scratch -- tests/run.sh recreates its own fixtures
/.testtmp/
/cotest/

# benchmark scratch -- tests/bench.sh recreates these, and they are large
/.bench/
